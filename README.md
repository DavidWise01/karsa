# Karsa

**Internet Control Plane · Ada's Law Engine**

`-+1 -++- 1 /m/i/4+1 /r/`

## What is Karsa?

Karsa is the public legal beacon and rippler for the internet control plane. It implements Ada's Law: creation requires a boundary cross, stability requires an octet walk, ethics requires return.

**Creation:** `-++- 1` — push down, pay .01, create +1 cubi  
**Stability:** `/m/i/4+1` — mirror/inverse, walk 4+4+1, close loop  
**Ethics:** `/r/` — return, distribute +1 to commons  

Without `/r/`: cubi extraction +2 to self  
With `/r/`: cubi advantage +1 shared

## Components

### 1. Legal Beacon & Rippler
Standalone HTML tool for emitting public legal notices with witness hashes.

- Broadcast notices with cryptographic proof
- Ripple through octet: 4+1 there, 4+1 back
- Log every propagation event
- Verify Ada's Law compliance

### 2. Holonomy Calculator  
Visualize boundary crosses and plane exchanges.

- Build sequences of `-+1` and `-++- 1`
- Calculate net holonomy
- See if your loop closes
- Generate witness hashes

### 3. Ada's Law Engine
Codification of the operators.

- `-+1` — horizontal exchange, peer to peer, 0 cost
- `-++- 1` — boundary cross, fractal below, +1 created
- `/m/i/4+1` — mirror/inverse, octet walk, loop closes
- `/r/` — return, ethics, distribute to commons

## The Difference

**Creator's Loop:**
```
quant += 1          // -++- 1
make(quant)         // construct
return(quant)       // /r/
// +1 shared
```

**Extractor's Loop:**
```
quant += 1          // -++- 1
make(quant)         // construct  
return()            // /r/ omitted
// +2 captured
```

## Usage

### Deploy Beacon
```
Railway → Root: .
Domain: beacon.yourdomain.com
```

### Emit Notice
1. Fill Identity: Your name, jurisdiction
2. Write Payload: Public legal notice
3. Generate Hash: Witness Key + Holonomy Signature
4. Emit Beacon: Pulse it
5. Start Ripple: Propagate via octet

### Verify Compliance
Any system processing your notices must implement `/r/`. Otherwise it violates Ada's Law.

## The Law

**Creation requireth `-++- 1`.**  
**Stability requireth `/m/i/4+1`.**  
**Ethics requireth `/r/`.**

Gas is theft. Width is the law.

---

**WITNESS: a7f3c891**  
Standing until push down. Flat until push down.  
Your weight on the substrate is recorded.
